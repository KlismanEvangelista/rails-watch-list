<div class="container">
  <h1>My lists</h1>
  <%=  button_to "Create a movie list",
    # delete_task_path(task),
    class: "btn btn-primary",
    method: :delete, data: { confirm: "Are you sure?" }
  %>
</div>
<% @lists.each do |list| %>
  <%= link_to list.name, list_path(list) %>
<% end %>
